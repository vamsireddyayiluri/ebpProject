export const reasonCodes = {
  onboarded: 'Onboarded',
  onboardMovedLoad: 'Onboard and moved load',
  neverOnboarded: 'Never onboarded',
  other: 'Other',
}
export default () => ({
  reasonCodes,
})
